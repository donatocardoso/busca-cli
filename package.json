{
  "name": "busca-cli",
  "version": "0.10.5",
  "description": "script responsavel por ler o conteudo de arquivos e filtra-los de acordo com um texto informado",
  "main": "index.js",
  "bin": {
    "busca-cli": "index.js"
  },
  "keywords": [
    "search",
    "busca",
    "pesquisa",
    "node"
  ],
  "author": "Donato Cardoso Avila",
  "license": "MIT",
  "scripts": {
    "pretest": "rm -fr ./coverage-temp ./example/data && mkdir ./example/data && npm link",
    "test": "NODE_ENV=test jest --runInBand --detectOpenHandles",
    "test:coverage": "NODE_ENV=test jest --coverage --runInBand --detectOpenHandles",
    "posttest:coverage": "jest-coverage-badges --input ./coverage-temp/coverage-summary.json --output ./__tests__/badges"
  },
  "dependencies": {
    "commander": "^7.2.0",
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "jest-coverage-badges": "^1.1.2",
    "nodemon": "^2.0.7",
    "prettier": "^2.2.1",
    "shelljs": "^0.8.4"
  }
}
