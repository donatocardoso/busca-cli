module.exports.message = (...messages) => {
  for (const message of messages) console.log(message);
};

module.exports.mostraMensagem = (...messages) => {
  for (const message of messages) console.log(message);
};
