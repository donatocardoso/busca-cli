const environment = require('./environment/environment');
const file = require('./file/file');
const folder = require('./folder/folder');
const load = require('./load/load');

module.exports = [environment, file, folder, load];
